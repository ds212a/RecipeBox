<UserControl
    x:Class="RecipeBox.UserControls.NewIngredientControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecipeBox.UserControls"
    xmlns:recipebox="using:RecipeBox"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="30"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0" x:Name="quantityTextBox" />
        <ComboBox Grid.Column="1" x:Name="unitOfMeasurementComboBox" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" PlaceholderText="Pick a measurement" />
        <TextBox Grid.Column="2" x:Name="nameTextBox" />
        <AppBarButton Grid.Column="3" x:Name="ingredientDeleteButton" Icon="Delete" />
    </Grid>
</UserControl>
